@using CMS.Domain.Enums;
@model CMS.Application.DTOs.InterviewsDTO

<h2>Edit</h2>

<form asp-action="Edit" asp-route-id="@Model.InterviewsId">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="form-group">  <input type="hidden" asp-for="InterviewsId" /> </div>

    <div class="form-group">  <input type="hidden" asp-for="StatusId" /> </div>
    <div class="form-group">
        <label asp-for="CandidateId" class="control-label">Candidate Name</label>
        @Html.DropDownListFor(model => model.CandidateId, (SelectList)ViewBag.candidateList, "Select a Name", new { @class = "form-control" })
        <span asp-validation-for="CandidateId" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="PositionId" class="control-label">Position</label>
        @Html.DropDownListFor(model => model.PositionId, (SelectList)ViewBag.positionList, "Select a Position", new { @class = "form-control" })
        <span asp-validation-for="PositionId" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="InterviewerId" class="control-label">Interviewer</label>
        @Html.DropDownListFor(model => model.InterviewerId, (SelectList)ViewBag.interviewersList, "Select an Interviewer", new { @class = "form-control" })
        <span asp-validation-for="InterviewerId" class="text-danger"></span>
    </div>



    <div class="form-group">
        <label asp-for="Date" class="control-label"></label>
        <input asp-for="Date" class="form-control" />
        <span asp-validation-for="Date" class="text-danger"></span>
    </div>


    @*  @if ((User.IsInRole("HR Manager") || User.IsInRole("Admin")) && Model.StatusName == "Pending")
    {

    }
    else
    {
    <div class="form-group">
    <label asp-for="Score" class="control-label"></label>
    <input asp-for="Score" class="form-control" />
    <span asp-validation-for="Score" class="text-danger"></span>
    </div>
    } *@
    @*
    <div class="form-group">
    <label asp-for="Score" class="control-label">Score</label>
    @if ((User.IsInRole("HR Manager") || User.IsInRole("Admin")) && Model.StatusName == "Pending")
    {
    <p>The score cannot be set for pending interviews by HR Managers or Admins.</p>
    }
    else
    {
    <input asp-for="Score" class="form-control" />
    <span asp-validation-for="Score" class="text-danger"></span>
    }
    </div>
    *@


    @* <div class="form-group">
    <label asp-for="StatusId" class="control-label">Status</label>
    @Html.DropDownListFor(model => model.StatusId, (SelectList)ViewBag.statusList, "Select an Interviewer", new { @class = "form-control" })
    <span asp-validation-for="StatusId" class="text-danger"></span>
    </div>*@
    @* <div class="form-group">
    <label asp-for="Notes" class="control-label"></label>
    <input asp-for="Notes" class="form-control" />
    <span asp-validation-for="Notes" class="text-danger"></span>
    </div>*@
    <div class="form-group">
        <input asp-for="AttachmentId" class="form-control" hidden />
    </div>

    <div class="form-group text-end " style="margin-top:10px;">
        <a class="btn btn-secondary" asp-action="Index">Back to List</a>
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>

</form>
<p>@Html.ValidationSummary()</p>
@*
<form asp-action="UpdateAttachment" asp-route-id="@Model.InterviewsId" enctype="multipart/form-data">
    <div class="form-group">
        <label for="file">Upload Attachment:</label>
        <input type="file" id="file" name="file" class="form-control-file" required />
    </div>
    <!-- Add other fields similarly -->
    <div class="form-group">
        <input type="submit" value="upload" class="btn btn-primary" />
    </div>

</form>
*@